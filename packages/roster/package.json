{
  "name": "@diagnostics/roster",
  "version": "0.1.0",
  "description": "Year-long staff shift roster management for the 6 Melbourne badminton outlets. Standalone module — separate business domain from the device-diagnostics product, deliberately not wired into @diagnostics/shared's Tenant/CustomerProfile model. In-memory store for now; see src/rosterStore.ts header for what a real deployment still needs (persistence, auth).",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "express-async-errors": "^3.1.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/jest": "^30.0.0",
    "@types/node": "^20.14.0",
    "@types/supertest": "^7.2.1",
    "jest": "^30.4.2",
    "supertest": "^7.2.2",
    "ts-jest": "^29.4.12",
    "tsx": "^4.16.2",
    "typescript": "^5.5.4"
  }
}
